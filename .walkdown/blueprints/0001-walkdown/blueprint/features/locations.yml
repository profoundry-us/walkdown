feature: locations
title: Where a project's pieces live, and why
source:
  prd: docs/08-locations.md
stories:
  - id: locations.default
    title: Adopting walkdown costs the repository nothing
    statement: As a person trying walkdown on a repository I did not start, I can run it without adding anything to the tree, and stop by deleting one directory nobody else can see.
    rules:
      - id: locations.default.nothing-in-the-tree
        origin: prd
        statement: With nothing configured, walkdown adds nothing to the repository at all. The home holding the spec, the threads, the runs, the evidence and the drafts lives in the person's own `~/.walkdown/blueprints/`, and the tree is exactly as it was found - no `.walkdown/`, not so much as a pointer - so that trying the tool commits a project to nothing and abandoning it means deleting one directory. Beside the code was tried and reversed - a `.walkdown/` in the tree, however well ignored, is still a thing git has to be told about, and the pointer it needed carried a home-directory path into shared history.
        verify: [checks]
        signoff: [eng, product]
        steps:
          given:
            - A repository that has never seen walkdown, and no personal config
          when:
            - walkdown is initialised and a run, a thread and a draft are recorded
          then:
            - The tree is byte for byte as it was - no `.walkdown/`, no pointer, nothing for git to see
            - Everything walkdown made sits in one numbered home under `~/.walkdown/blueprints/`
            - Deleting that one directory removes everything walkdown made
            - Asked what is tracked, walkdown answers nothing, and says why
          statement_hash: sha256:7a82184b4b5d

      - id: locations.default.records-follow-the-spec
        origin: prd
        statement: A home holds the spec and its four kinds of record as siblings - `blueprint/`, `threads/`, `runs/`, `evidence/`, `drafts/` - one layout wherever the home sits, so that a home moves between the personal `.walkdown` and a repository's as one directory and nothing in it needs rewriting. Which siblings git sees is the commit standard's business and not the layout's - runs, evidence and drafts are what a machine or one person produced, and a committed spec keeps them out unless everything was asked for.
        verify: [checks]
        steps:
          given:
            - A blueprint in its home, wherever the home sits
          when:
            - Its location is asked for
          then:
            - The spec and the four records resolve as siblings in one home
            - No kind of record sits inside another
            - Moving the home moves all five together and edits none of them
          statement_hash: sha256:3272ecc28575

      - id: locations.default.in-repo-on-request
        origin: prd
        statement: A project that wants walkdown's files in version control says so, and there are two answers worth offering - commit the spec and the threads that go with it, or commit everything including the runs and the evidence. Neither is the default and each is one flag, and what was chosen is remembered nowhere - it is read off the tree. The home sits under the repository's `.walkdown/`, and a `.gitignore` beside it is the spec standard - delete the file and everything is committed, write it back and it is not. Changing your mind is the same command again, and it changes the tree to say so, moving the home whole in either direction, and reports what is tracked now and what is not - because a tool that quietly adds a project's screenshots to its history has not been polite, and one that prints success without changing anything is worse.
        verify: [checks]
        signoff: [eng, product]
        steps:
          given:
            - A project, fresh or already set up
          when:
            - The person asks for the spec to be committed, for everything to be, or for nothing to be again
          then:
            - The home sits under the repository's `.walkdown/`, laid out as every home is, and the pointer names it by a relative path
            - With the spec committed, a `.gitignore` beside the home keeps runs, evidence and drafts out - and that file, and the config, are themselves committable
            - With everything committed there is no `.gitignore` there at all
            - Asked again with a different standard, the tree changes to say so - the file is written or removed, or the home moves whole with no record edited
            - The command reports what is tracked now, in words, and how to change its mind later
            - What is tracked is asked of git, not read off the ignore file walkdown wrote - a root .gitignore hiding .walkdown, a rule that does not reach a blueprint standing elsewhere, an ignore file somebody emptied, and a home that left with its files still in the index are each said outright, and lint errors wherever the tree's promise and git's answer disagree (n-0179, n-0180, n-0181)
          statement_hash: sha256:d66c40335b64

      - id: locations.default.skills-are-yours-by-default
        origin: prd
        statement: The agent procedures walkdown ships install into the person's own skills directory, where they work in every project on the machine and add nothing to any repository - and a project that commits its spec gets them committed beside it instead, because skills follow the spec for the same reason its runs and threads do. A procedure whose harness the receiving project does not have is not shipped at all.
        verify: [checks]
        signoff: [eng, product]
        steps:
          given:
            - A project being set up, and a machine with a personal skills directory
          when:
            - The skills are installed, either by init or on their own
          then:
            - With nothing committed they land in the person's own directory and the tree gains nothing
            - With the spec committed they land beside it, to arrive with a clone
            - A skill that depends on a harness only walkdown's own repository has is not among them
            - A copy the person has edited is kept and reported rather than overwritten
          statement_hash: sha256:62223922d07f

      - id: locations.default.one-home-per-blueprint
        origin: thread:n-0124
        statement: Every blueprint gets a numbered home of its own inside the `.walkdown` that answers for it - `.walkdown/blueprints/0002-name/` - and two blueprints never share one, because the number is allocated against that directory's own listing rather than derived from a name two projects can both have. No path walkdown resolves is keyed by a name somebody else could also choose - and neither is the question of which two config rows are one project, which is answered by where an entry is rooted and never by its id alone. One blueprint is never given a second home with its records stranded in the first. Asking where things live allocates nothing and writes nothing. A home that already holds records keeps answering for its blueprint, and moving one stays a decision a person makes rather than something a resolver does on its own.
        verify: [checks]
        signoff: [eng, product]
        steps:
          given:
            - Two blueprints in one `.walkdown`, and two repositories whose directories share a name
          when:
            - Each is asked about, then written to for the first time
          then:
            - Asking reports where records would go and writes nothing, not even the claim on that home
            - Each blueprint ends up with a numbered home of its own, and no two ever coincide
            - Nothing resolves through a name two projects could share, whichever door does the resolving - not the config merge, not a server's list, not a move
            - Two checkouts that share a name, one listed personally, stay two projects with two ledgers
            - A home that already holds a blueprint's records keeps answering for it
            - A home nothing claims is reported and left standing, never guessed at and never moved
          statement_hash: sha256:c2e4b20e7f0d

  - id: locations.answer
    title: Anyone can ask where this project's pieces are
    statement: As someone standing in a repository, I can ask where every piece lives and why it was chosen there, and the asking never changes the answer.
    rules:
      - id: locations.answer.says-why
        origin: prd
        statement: Every path walkdown reports comes with the reason it was chosen - named on the command line, this machine's config, already in the blueprint, the built-in default - because a person who disagrees with where something is needs to know which decision to argue with.
        verify: [checks]
        steps:
          given:
            - A project with some paths defaulted and some configured
          when:
            - walkdown is asked where things live
          then:
            - Each of the spec, runs, threads, evidence, drafts and code carries its reason
            - The reason names the rule that decided it, not merely that a decision was made
          statement_hash: sha256:971e878234f9

      - id: locations.answer.asking-writes-nothing
        origin: prd
        statement: Asking where something lives creates nothing - no directory, no config file, no placeholder - so that the command a confused person reaches for first cannot itself change what they were confused about.
        verify: [checks]
        steps:
          given:
            - A project with no personal config at all
          when:
            - walkdown is asked where every kind of record lives
          then:
            - It answers in full, naming directories that do not exist yet
            - The disk is exactly as it was found, including the personal home
          statement_hash: sha256:0ba6f381a575

      - id: locations.answer.nearest-blueprint-wins
        retired: >-
          Answered by deleting the question (2026-09-01, q-0138). Walkdown no longer searches the tree for blueprints, so there is no tree answer to arbitrate against a configured one and no precedence left to state. The half of this that survived - a repository may hold several blueprints without them bleeding into each other - is now locations.answer.declared-not-discovered, which carries it without the contest.
        origin: prd
        statement: A blueprint found in the working tree beats a configured entry rooted above it, and a blueprint named outright is answered by the entry that owns that blueprint or by none at all - because a repository can hold several, and an entry rooted at the whole tree would otherwise answer for its own siblings and report one project's spec beside another project's ledger.
        verify: [checks]
        steps:
          given:
            - A repository holding two blueprints, and a config entry rooted at the whole repository
          when:
            - The location is resolved from inside the inner one
          then:
            - The inner blueprint answers, with its own runs and threads
            - The outer entry still answers where the tree offers no blueprint, which is what an out-of-tree spec is for
          statement_hash: sha256:8abbb2442baa

      - id: locations.answer.one-walkdown-answers
        origin: walkdown
        statement: Exactly one `.walkdown` directory answers for where you are standing - the nearest one at or above you - and it is merged with the personal `~/.walkdown` and with nothing else. A pack carrying its own `.walkdown` never sees the repository root's, and the root's never sees the pack's, because a monorepo is several projects that happen to share a checkout, and a tool that pooled them would let one pack's board list, serve and write to another pack's ledger. Only blueprints under the `.walkdown` that answered are listed, served or written to.
        verify: [checks]
        signoff: [eng, product]
        steps:
          given:
            - A repository holding a `.walkdown` at its root and another inside a pack, and a personal `~/.walkdown`
          when:
            - A location is resolved from inside the pack, and again from the repository root
          then:
            - From the pack, the pack's own answers and the root's is not consulted at all
            - From the root, the root's answers and the pack's is invisible
            - Each is merged with the personal one, and with no second repository config however many lie above it
            - A server started in one of them offers only the blueprints that `.walkdown` declares, wherever the blueprint it serves happens to sit
            - A committed entry whose spec lies under another `.walkdown` is refused when written and reported by lint when found - that `.walkdown` already answers for it (q-0168)
            - One that is there anyway is not read either - where names it as refused and says which directory answers for it, and status and serve never see it - and an ephemeral entry may not name it from outside, because a copy means a copy and this is the original (q-0176)
          statement_hash: sha256:b08354a355f7

      - id: locations.answer.declared-not-discovered
        origin: thread:q-0138
        statement: Every blueprint walkdown answers for is one somebody wrote down - in the `.walkdown` that answers for where you are standing, or in the person's own - and never one it went looking for, so a repository may hold several without them bleeding into each other.
        verify: [checks]
        signoff: [eng, product]
        steps:
          given:
            - A repository holding two blueprints, each declared, and a third nobody declared
          when:
            - The location is resolved from inside each of them, and one is named outright
          then:
            - Each declared blueprint answers for itself, with its own runs and threads
            - The undeclared one is not a project at all, however much it looks like one from the outside
            - The shared config and the personal one are both consulted, and the report says which answered - however many keys the person restated
          statement_hash: sha256:dd87f8781211

  - id: locations.keeping
    title: A ledger already written is never orphaned or edited
    statement: As a team with history in our blueprint, no preference of anyone's machine can make our records stop being found, and moving them stays something a person did on purpose.
    rules:
      - id: locations.keeping.existing-outranks-preference
        origin: prd
        retired: >-
          The rule was about records found INSIDE a blueprint - `blueprint/runs/`,
          the layout every project had before homes - outranking a blanket
          default. That layout is not read any more: every blueprint walkdown
          answers for is declared, lives in a home, and its entry names the
          records or the home they derive from, so there is no directory for
          the resolver to discover and nothing for a preference to give way to
          (2026-09-04, Topher's call to remove legacy support entirely). What
          survives of it is locations.keeping.moving-is-a-decision: relocating
          records is still something a person does, never something a default
          does to them.
        statement: A blanket default never points past records a blueprint already holds - an existing directory is a fact and a default is a preference, so the preference gives way and moving those records stays a decision somebody makes.
        verify: [checks]
        steps:
          given:
            - A blueprint whose runs directory already holds records
          when:
            - A personal config declares a different default for runs
          then:
            - The existing directory is still the answer, and the report says it is because the blueprint already holds it
            - No record is moved, copied or hidden by the resolution
          statement_hash: sha256:c5603d183d94

      - id: locations.keeping.moving-is-a-decision
        origin: prd
        statement: Moving records relocates the files unchanged, remembers the choice in the personal config beside the person's own comments, and refuses a destination that already holds records - because two ledgers interleaved into one directory is an edit of both, and this ledger is append-only.
        verify: [checks]
        steps:
          given:
            - A blueprint whose runs live in the repository
          when:
            - They are moved somewhere else
          then:
            - Every file arrives byte for byte, and none is rewritten to suit its new address
            - The personal config records where they went, keeping whatever the person had already written in it
            - A destination that already holds records is refused rather than merged into
          statement_hash: sha256:452142df8095

  - id: locations.travel
    title: A record means the same thing on somebody else's machine
    statement: As a second person cloning this project, the verdicts I read were made against a spec I can identify and point at evidence I can actually open, without anyone having to have used my directory layout.
    rules:
      - id: locations.travel.evidence-by-key
        origin: prd
        statement: A run names the evidence it left by a logical key rather than a filesystem path, and the server resolves that key per machine - so a screenshot filed on one laptop is still findable from another, and no ledger entry is pinned to the disk of whoever recorded it.
        verify: [checks]
        steps:
          given:
            - A run recorded while evidence lived in the repository
          when:
            - Evidence is moved out of the repository and the run is read again
          then:
            - The same key still resolves, and the picture still opens
            - No run file was edited to make that true
            - A key that climbs out of the evidence directory is refused
          statement_hash: sha256:a7bc7cd07be3

      - id: locations.travel.judged-against-a-spec
        origin: prd
        statement: Every run records a content hash of the spec it was judged against, computed from the blueprint's own files, so that a verdict says what it was measured against whether or not the project has a repository - and recording a verdict never changes that hash, because a run is what the spec produced rather than part of it.
        verify: [checks]
        steps:
          given:
            - A blueprint and a run recorded against it
          when:
            - A verdict, a thread and a draft are written, then a rule is reworded
          then:
            - The three writes leave the spec hash exactly where it was
            - The rewording moves it
            - The same words in a different feature file are a different spec
          statement_hash: sha256:5014abd76d50

      - id: locations.travel.provenance-not-currency
        origin: prd
        statement: What code a run saw - the repository's sha, and a hash of the working tree when it was dirty - is provenance, never currency. It tells a person digging into a verdict where to look, and never decides whether that verdict still counts. Whether a verdict counts is answered per cell, by the statement it was made against, the check that still claims it, and any sweep since - so an unrelated commit does not turn a board green to grey.
        verify: [checks]
        steps:
          given:
            - A ledger of passing verdicts
          when:
            - Commits land that touch neither the rules nor the code they describe
          then:
            - Every cell reads exactly as it did before
            - The runs still carry the shas they were recorded with, for anyone who wants to look
          statement_hash: sha256:c513160f49fc

  - id: locations.pointer
    title: An agent arriving in the repository finds the spec
    statement: As an AI agent cloning this project, I find one paragraph telling me a spec exists and where it is - in the file this project actually keeps its conventions in, saying where the spec is now rather than where it used to be.
    rules:
      - id: locations.pointer.placed-where-agents-read
        origin: prd
        statement: The pointer goes in a file this project already keeps its agent conventions in, and walkdown chooses only where there is no question - none, so it writes one; exactly one, so the project has already answered; several, so it names them and writes nothing. Which file agents read is a project's own business, and a sentence put in the wrong one is a sentence nobody reads.
        verify: [checks]
        signoff: [eng, product]
        steps:
          given:
            - A repository being initialised with its spec committed - an uncommitted one gets no pointer, since the pointer would be the one thing in the tree
          when:
            - It holds no agent-instruction file, or exactly one, or several
          then:
            - With none, one is created carrying the pointer
            - With one, the pointer goes into it and no second file is made to compete with it
            - With several, none is written to and the report names them for a person to choose between
          statement_hash: sha256:66759d1c478c

      - id: locations.pointer.owns-only-its-block
        origin: prd
        statement: walkdown owns the fenced block and nothing else in that file. It rewrites what lies between its own markers, leaves every other line as it was, and updates the block when the spec moves - because a pointer still naming a directory the spec has left is worse than no pointer at all, since an agent believes it and goes looking.
        verify: [checks]
        steps:
          given:
            - An agent-instruction file with a person's own words above and below a walkdown block
          when:
            - The spec moves and the pointer is placed again
          then:
            - The block names the new place, and appears exactly once
            - Every line outside the markers is untouched
            - Placing it twice in a row changes nothing the second time
          statement_hash: sha256:ec57ac3e3cc3
