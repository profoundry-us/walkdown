id: q-0002
kind: question
author: agent
created: 2026-08-21T00:14:00Z
anchor:
  rule: waitlist.join.already-joined
  screen: waitlist-already
status: open
body: >
  Design request: the spec now requires an "already on the list" state
  (rule waitlist.join.already-joined, incorporated from q-0001), but the
  prototype has no screen for it. A proposal sketch is on file at
  proposals/waitlist-already.html — adopt, redraw, or replace it in the
  design tool. When the designed screen ships, set this screen's
  `prototype:` path in the storyboard and mark this thread incorporated.
